import 'package:flutter/material.dart';

class PhotosolonsList extends StatelessWidget {
  const PhotosolonsList({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('PhotosolonsList'),);
  }
}
